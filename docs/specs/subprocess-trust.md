# Subprocess Trust Classes and Launch Contracts

- **Document role**: normative cross-cutting specification
- **Specification version**: 1.0.0
- **Delivery status**: contract delivered; hardening gaps remain tracked separately
- **Owner**: mini-agent maintainers
- **Last reconciled**: 2026-08-02

The corpus authority and conflict rules are defined in [`00-index.md`](00-index.md). This document
controls how mini-agent selects a subprocess boundary and records the authority crossing that
boundary. [`phase-2-sandbox.md`](phase-2-sandbox.md) continues to control the concrete `bwrap` and
Seatbelt capability matrices for model-generated actions.

## Core invariant

A subprocess profile is selected from **who authored the executable/arguments** and **what
authority the child needs**. The mere fact that code uses `Command`, `.spawn()`, `.output()`, or
`.status()` never determines the profile.

Every production launch must name exactly one trust class below before it can merge. Unknown or
ambiguous authorship, an unclassified launch site, invalid argv for the selected grammar, and an
unavailable requested containment backend all deny launch. A caller must not silently retry under
a more privileged profile.

## Required launch contract

The owner of a launch site must make all of these fields reviewable, even when the value is
"ambient", "not required", or "unsupported":

1. stable trust-class identifier and launch-site owner;
2. authoring principal for executable, arguments, and any interpreted script;
3. argv grammar, including which fields are opaque shell text and which are direct arguments;
4. working directory and how it is resolved;
5. environment construction and credential inheritance;
6. required filesystem and network authority;
7. sandbox/containment state and requested-backend behavior;
8. permission decision and a collision-free, durable audit identity;
9. wall-clock, stdout, stderr, and combined-output limits;
10. caller cancellation, direct-child reaping, and descendant/tree cleanup; and
11. supported platform behavior, including explicit gaps.

User-controlled values must remain data in a direct argv grammar. A profile that permits an opaque
shell program must say so explicitly; quoting a value does not convert that grammar to direct
exec. Configuration trust and command execution permission are separate decisions. The model-action
shell is resolved once from the canonical workspace and the invocation's captured `PATH`. Only
Bash/sh (`-c`) and, on Windows, PowerShell/pwsh (`-Command`) are supported. The canonical executable
identity is held and revalidated at launch; missing, replaced, or unsupported shells are not
registered or described to the model and never fall back to an unchecked name.

## Trust classes and normative contracts

"Current gap" text is an audit result, not permission to preserve the gap. Follow-up hardening may
narrow a class without changing its identity, but broadening authority requires this specification
to change first.

| ID and current launch sites | Owner / principal and argv grammar | CWD | Environment / credentials | Filesystem / network | Containment and requested backend | Permission / audit | Resources, cancellation, and cleanup | Platform status |
|---|---|---|---|---|---|---|---|---|
| `TC-MODEL-ACTION` — `BashTool::call`; brokered JS `SpawnEffectService`; shared constructors in `Sandbox::wrap_command`; terminal launch in `Sandbox::run_built_output_command` | Model authors the Bash script or JS `cmd`/`args`. Bash uses the configured shell with opaque command text. Unix JS uses fixed `exec "$0" "$@"`; Windows JS sends structural program/argv through the inherited helper-request handle. | Canonical current process workspace at launch; every supported wrapper sets it explicitly. | Requested `bwrap`, Seatbelt, and AppContainer launch clear ambient credentials and restore only their documented narrow allow-lists. `zerobox` remains backend-defined. An explicit unsandboxed choice inherits ambient environment. | Linux/macOS deny IP network and enforce their Phase 2 filesystem policies. Windows adds package-SID grants for the canonical workspace, application cache, exact selected executable, and bounded configured roots, while regular-AppContainer resources already readable to `ALL APPLICATION PACKAGES` remain visible; AppContainer network capabilities are empty. `zerobox` is backend-defined. | Shared `Sandbox`; never a raw JS launch. Windows uses a fixed-function same-executable helper, unique AppContainer, handle-bound ACLs, locked executable proof, private desktop, and creation-time UI-restricted Job; it remains separate from LPAC. Requested-unavailable never retries unsandboxed. | Mandatory `bash` permission for the complete Bash script or structural JS program/argv identity; denial/cancellation has no effect. JS approval and policy identities preserve argv boundaries. Sandbox capability state is user-visible. | 30 s maximum with bounded output. Unix kills and drains the owned process group; Windows waits for the exact Job to report zero active processes before ACE/profile cleanup and retains recovery state when safe cleanup cannot be proven. The cached Windows startup preflight has a five-second run deadline, up to five seconds for whole-tree reaping, and a fresh five-second profile/ACL recovery ceiling; it synchronously owns its helper and publishes a durable profile intent before profile creation. Windows `CreateProcessW` itself is not cancellable; if that OS call returns after the run deadline, the still-owned child is rejected and reaped before the probe returns. | Linux and macOS guarantees are exactly those in Phase 2. Windows becomes available only after its native explicit-root/network/Job attestation and cached production preflight succeed. |
| `TC-BROKER-JS-WORKER` — Linux `sandbox::worker::linux` preflight and production launch | mini-agent selects one trusted `bwrap`, the canonical current executable, a fixed internal marker, and no model/user-authored argv. The contained worker accepts only the framed parent protocol; model-requested effects remain data returned to the parent broker. | Private `/tmp` inside an empty bubblewrap root. | `env_clear` plus bubblewrap `--clearenv`; only the fixed internal worker marker is restored. No ambient credential, configuration, workspace, home, or `PATH` variable crosses the boundary. | Exact worker image and exact root-owned, non-group/other-writable regular files already mapped into the system runtime only; shared objects do not require `+x`. Private proc/dev/tmp; no workspace, cache, or config mount; a new network namespace has no host socket reachability. | Dedicated broker-only bubblewrap profile, never `Sandbox::wrap_command`. User/PID/network/IPC/UTS namespaces, all capabilities dropped, close-fds, die-with-parent, and new-session are required. After authenticated `Hello` and before `Ready`, rlimits, non-dumpability, `no_new_privs`, and seccomp are validated. Exact process/network/namespace denials are supplemented on x86_64 by rejecting the complete x32 syscall-number range. Any failure emits no `Ready`, reports unavailable, and never retries uncontained. | Internal worker creation is authorized only by the registered JS runtime. Typed containment availability and protocol outcomes are the audit surface; arbitrary worker exception/source data remains redacted. Parent-brokered effects retain their own permissions. | Five-second preflight; supervisor-owned request deadlines; 256 MiB address space, 35 s CPU, 64 descriptors, zero core, non-dumpability, and 1 MiB file-size ceilings. Runtime evidence requires exact SIGXCPU enforcement and a sacrificial SIGABRT child with `core_dumped() == false` and no artifact, including under a piped host `core_pattern`. The parent owns all three pipes and a process group; a post-Ready protocol fault and the exact controlled sleeper PID/start-time prove kill/reap cleanup. | Linux only. Production is available only after a real runtime preflight succeeds. The ignored `linux_js_worker_containment` test is the Linux+bubblewrap evidence gate; macOS source tests cannot upgrade Linux runtime evidence. |
| `TC-BROKER-JS-WORKER` — `sandbox::worker::macos` | mini-agent selects its current executable, descriptor-publishes one exact byte-proven one-time image, and supplies only fixed guardian/worker markers and protocol argv; no model/user argv is accepted. | The inherited cwd grants no authority under the deny-default worker profile; live sentinels prove workspace and private-store denial. | `env_clear` at both transitions; only fixed internal markers and bounded preflight canary paths cross the guardian boundary. Ambient credentials, configuration, home, and `PATH` are absent. | The worker can read/execute only its exact one-time image plus required system runtime files, use anonymous protocol FIFOs, and query narrow self/system metadata. Workspace, skill-store, credential sentinels and IPv4/IPv6 TCP/UDP connections are denied. | A trusted current-binary guardian owns a dedicated process group and launches the one-time image under a dedicated deny-default Seatbelt profile. Authenticated Ready causes descriptor/inode-verified unlink before request sequence 2. The worker attests fork/exec, network, descriptor, limit, and group controls; any failure reports unavailable and never retries uncontained. Assurance remains `DeprecatedBestEffort`; hostile same-UID peers are outside the scoped boundary. | Internal worker creation only. Status exposes typed backend/assurance/reason and a closed source-free preflight result; parent-brokered effects retain their independent grants, permissions, and audit. | Five-second protocol/lifecycle phase deadlines plus a 30-second finite stale-sweep lock-contention bound; 40 GiB virtual address space (required by the nonresident dyld shared-cache mapping), independent 64 MiB QuickJS allocation cap, 35 s CPU, 64 descriptors, zero core, and 1 MiB file size. Parent heartbeat EOF kills the complete group; shutdown reaps the exact worker and retires its lease/directory; bounded stale recovery proves parent-death cleanup. | macOS 26 only. Production is available only after the complete live preflight succeeds and reports weaker deprecated assurance. Other or unvalidated majors remain unavailable. |
| `TC-BROKER-JS-WORKER` — `sandbox::worker::windows` production launch and attestation | mini-agent selects the supported current executable, fixed internal marker, and exact anonymous protocol handles. No model/user argv is accepted. | LPAC process with no workspace cwd authority. | Unicode environment block contains only the fixed worker marker; credentials are absent. | Zero-capability LPAC is a token boundary, not a filesystem namespace: host objects whose ACLs grant the stable package identity access can remain visible. The minimal local attestation does not test ambient workspace/skill-store, credential, loopback-network, actual-child, omitted-handle, or install-root denial. Hosted canaries record those observations only on their reference runner. | Exact `HANDLE_LIST`, compatible mitigations, creation-time Job assignment, active-process limit one, memory/CPU/UI limits, and kill-on-close. A process-wide `OnceLock` reports available only after the same-launcher attestation verifies parent Job/mitigation state, child LPAC/token/handle/console/device state, a closed protocol probe, `6 * 7 == 42`, shutdown, and successful exit. Startup and `--print-config` status preflight create/reuse a persistent AppContainer profile and may add a persistent exact RX ACE to a supported user-owned installed executable; no automatic cleanup, rollback, or consent prompt exists. | Internal worker creation only; parent-brokered effects retain independent grants, permission, and durable audit. CI run 31319107422 records the full canary and supported install matrix for its reference runner; those observations do not grant runtime authority on another host. | Job close owns whole-tree termination and reaping. The OS creation call runs on one owned helper thread behind a five-second caller deadline but is not itself cancellable; a late result is torn down and a permanently blocked call cannot trigger a second launcher. | Windows: available only after local attestation. CI run 31319107422 records the hosted and standard-user observations on its reference runner. LPAC worker containment never authorizes general JS `spawn`; model-authored spawn uses the separately attested regular AppContainer, while learned-skill spawn remains disabled without an immutable-executable snapshot backend. |
| `TC-EXPLICIT-USER-SHELL` — `Startup::dispatch_print`, `App::run_bang_command`, and `Sandbox::run_explicit_shell` | Human authors the opaque text after `!`; the complete suffix, including whitespace, is passed unchanged to the configured shell. This is not model-generated authority and never enters BashTool permission grammar. | Current process workspace is captured immediately before construction and applied explicitly to the child/wrapper. | A selected `bwrap`, Seatbelt, or AppContainer boundary clears ambient credentials according to the general sandbox capability matrix. Explicit trusted bypass and unavailable-default fallback remain distinct reported states. | Authority is exactly the selected general sandbox policy; unsandboxed states inherit ambient host filesystem and network access. | Uses the configured general `Sandbox`, never the broker-only worker profile. Requested-unavailable starts no child and never retries unsandboxed. | Typing `!` is the authorization event. The shared audit records command identity, class, cwd, boundary, and outcome without environment values or output. | 30 s with bounded capture. Unix owns the process group; Windows AppContainer owns the complete Job and drains it before cleanup. | Uses the configured shell contract. Unix process-group and Windows AppContainer Job teardown follow the same fail-closed lifecycle policy. |
| `TC-LOOP-VALIDATION` — shared `extras::loop::validation` calls from headless and interactive loops | Human authors `--loop-run` opaque shell text. The selected `Sandbox` supplies its configured shell grammar; model output never becomes this command. | Canonical current process workspace selected by the general sandbox wrapper. | Requested bwrap/Seatbelt/AppContainer uses the general minimal environment; explicitly unsandboxed validation inherits the ambient toolchain environment. | Linux/macOS deny network; Windows adds only the explicit AppContainer roots, retains pre-existing `ALL APPLICATION PACKAGES` visibility, and supplies no network capability. | Not the broker-only worker profile. The selected general backend is fail-closed when requested; there is no ambient retry. | CLI configuration authorizes the command and identifies iteration, cwd, containment, and outcome. | Shared finite timeout/output limits and operation-scoped cancellation own the Unix process group or Windows Job without cancelling unrelated commands. | Shell grammar follows the configured platform shell; Windows runs through the AppContainer helper after native preflight succeeds. |
| `TC-PROJECT-AUTOMATION` — `run_hook_with_policy`, its bounded helper, and `run_shell_condition` | Managed/global hook owner or human-confirmed project binding authors it. Handler `command` + required `args` is direct exec. Relative executable paths resolve from the canonical selected workspace. An `if` condition explicitly uses opaque `sh -c`/PowerShell text. Sandboxed direct execution uses only a fixed, quoted readiness launcher before `exec`; hook argv is never parsed as shell text. | Trust filtering and project-hook approval remain bound to the immutable canonical startup project root. Worktree selection separately supplies an identity-pinned canonical execution root for both conditions and handlers; `$ZEROSTACK_PROJECT_DIR` and the stdin envelope name that same selected workspace. The selection generation and filesystem identity are revalidated before envelope construction and immediately before every child creation, including again after a condition completes. A failed rebind, concurrent completed rebind, missing/replaced directory, filesystem root, or identity mismatch denies launch. The parent process cwd cannot retarget either binding. | Ambient environment is cleared. A non-credential runtime allow-list, immutable `ZEROSTACK_PROJECT_DIR`, and literal handler `env` values are constructed explicitly. Invalid/reserved/case-colliding keys deny launch; diagnostics never contain values. | Default `trust: sandboxed` uses the general workspace profile: Linux bwrap provides minimal runtime reads, workspace/cache writes, and denied IP network; macOS Seatbelt truthfully retains host reads while limiting writes and denying network. `trust: trusted` explicitly consents to ambient filesystem/network without restoring ambient credentials. | Separate trusted automation runner, not `TC-MODEL-ACTION` and not `TC-BROKER-JS-WORKER`. `sandboxed` uses the selected general backend and requested-unavailable/invalid containment denies before child creation; it never retries ambiently. A trusted inner launcher verifies target visibility and signals readiness immediately before `exec`; missing readiness makes wrapper setup/pre-exec failure a policy denial, not an ordinary hook exit. `trusted` is the only named containment bypass. | Managed/global source policy plus hash-bound interactive trust for project executable argv, condition, trust, and explicit env; the prompt exposes a value-free digest of the environment binding, and headless unconfirmed project hooks are skipped. Every launch logs event, executable, trust, containment, filesystem, and network status without argv/data/secrets. Policy denial blocks synchronous blockable dispatches; async and observation-only events retain their declared no-decision semantics. | Configured timeout; 1 MiB stdout, 1 MiB stderr, 1.5 MiB combined; output failure, timeout, future cancellation, and normal completion clean the Unix process group. Async work remains structurally scoped to dispatch cancellation. `once` is consumed only after confirmed in-containment readiness; false conditions, dispatch cancellation, and pre-launch/wrapper failures leave it retryable. **Current gap:** Windows descendant cleanup is not proven. | Direct exec is portable; condition shell differs by platform. Linux bwrap and macOS Seatbelt claims are runtime-reported. Windows has no verified containment backend, so default sandboxed hooks deny; explicit trusted hooks retain minimal-env/direct-exec behavior but Windows tree semantics remain unsupported. |
| `TC-MCP-STDIO` — `McpClientHandle::connect` command arm, `stdio_command`, `Sandbox::wrap_workspace_service`, and `TokioChildProcess::builder(...).spawn()` | Human-trusted MCP configuration authors program and direct args. PATH resolution occurs once before spawn and the resolved absolute executable identity is passed without a shell. The remote model only chooses exposed MCP tools; it does not author the server launch. | Configured `cwd`, resolved relative to the connection workspace; omission captures and explicitly applies that workspace. | Ambient environment is cleared. `env` delegates explicit values; `inherit_env` names individual approved parent values, with explicit values winning. Neither values nor stderr beyond the diagnostic cap are logged. | Workspace-capable. Top-level `network = "inherit" | "deny"` records service authority separately from tool permission. Uncontained `inherit` uses the host network; `deny` requires an enforcing backend and otherwise starts no child. | Omitting `sandbox` is a truthful human-trusted bypass, not an active-boundary claim. A requested backend uses the dedicated direct-exec workspace-service profile and fails closed when missing, unsupported, or unable to enforce requested network denial. It never reuses broker-worker containment. | Configuration trust authorizes server start; MCP tool calls retain their own permission/audit identity. Built-in remote identities do not turn deserialized command servers into built-ins. Sandbox and network state never grant tool-call permission. | 10 s initialize deadline and at most 8 KiB rendered diagnostic stderr. The RMCP adapter owns `KillOnDrop` plus a Unix process group or Windows Job Object and explicitly terminates the group after graceful direct-child cleanup because a descendant may close protocol pipes first. Init error/timeout, cancelled connect, reconnect replacement, explicit shutdown, and handle drop cancel, kill, and reap the tree. | Unix process groups and Windows Job Objects are selected at compile time through `process-wrap`. Tests exercise Unix descendants; Windows Job Object parity is provided by the transport wrapper and remains a required platform CI row. |
| `TC-LSP-SERVICE` — `LspClient::spawn`, `lsp_command`, and `sandbox::owned_workspace_service_tree` | Built-in registry or trusted user LSP configuration authors program and direct args. PATH resolution occurs once before spawn and the resolved absolute executable identity is passed without a shell. Model edits trigger lazy startup but do not author argv. | Canonical session workspace root, applied identically as child cwd, sandbox exposure, and initialize `rootUri`. | Ambient environment is cleared. `env` delegates explicit values; `inherit_env` names approved parent values, with explicit values winning. Built-ins delegate only PATH for project toolchain discovery. Values and raw stderr are never logged. | Full project/workspace reads and common build-cache writes. `network = "inherit" | "deny"` records service authority; denial requires an enforcing backend and otherwise starts no child. | Omitting `sandbox` is a truthful trusted-service bypass. A requested backend uses the direct-exec workspace-service profile, fails closed when unavailable or insufficient, and never reuses broker-worker containment. | Enabling trusted LSP config authorizes launch; audit identifies server name, executable identity, root, and lifecycle outcome. **Current gap:** trace logging is not a durable audit record. | 15 s initialize deadline; 8 KiB headers and 8 MiB inbound/outbound bodies; fixed-buffer stderr terminated after 64 KiB; document sync limited to 4 MiB without speculative version changes. Diagnostics are workspace-scoped and retained for at most 128 files per server with 50 sanitized entries per file; cap breach terminates the server. A supervisor owns `KillOnDrop` plus a Unix process group or Windows Job Object. Init failure/timeout, malformed transport, cancellation, exit, manager replacement, shutdown, and drop signal whole-tree kill and reap. Failed/dead clients are not cached, enabling clean restart. | Unix process groups and Windows Job Objects are selected at compile time through `process-wrap`; the lifecycle fixture is required in Linux, macOS, and Windows CI. |
| `TC-INTERNAL-GIT` — `GitRunner::run` in `extras::git_worktree` (used by detect, create, merge/cleanup/conflict helpers, status, and auto-commit); `Session::detect_git_status`; `/undo` stash | mini-agent authors a closed git subcommand grammar. User/worktree paths and branch names remain individual argv values, never shell text; branch operands are validated with `check-ref-format --branch` and option-safe separators. `/wt-merge` and auto-merge both dispatch this typed grammar; neither reconstructs a shell command or delegates Git mutation to a conversational agent. | Worktree operations always use `git -C <canonical-path>`; no worktree flow mutates process-global CWD. Worktree switches atomically rebind the session path, context-discovery root, permission root, agent preamble, sandbox child CWD, and relative file-tool roots. The cleanup layer refuses to delete the process's active workspace while holding the canonical repository lock. Legacy session and `/undo` sites still use their selected current repository. | Ambient Git credentials and ordinary configuration are retained for remote access, while repository-redirecting `GIT_DIR`, `GIT_WORK_TREE`, common-dir, index, object-directory, namespace, and prefix variables are removed so they cannot override the explicit repository. | Selected repositories/worktrees; network is needed only by explicit fetch/pull operations. | Worktree Git uses the shared owned-child lifecycle worker without selecting a model-action sandbox. Optional containment must preserve selected repositories and credential-helper behavior and fail closed when requested. | UI/CLI operation is authorization. Worktree execution centralizes normalized operation names and fails closed unless the canonical common Git directory can be established; a durable shared audit sink remains a current gap. | Worktree queries: 10 s and 256 KiB per stream/384 KiB combined. Local mutations: 60 s and 512 KiB per stream/768 KiB combined. Fetch/pull: 120 s with the same mutation output caps. Timeout and output overflow kill and reap the owned child lifecycle. Create and merge supervisors treat response-receiver drop as transaction cancellation. Failed creation removes a registered worktree only after its exact branch, HEAD, ref, and clean status are proven; dirty or unverifiable hook output is retained. Branch reservation creates a unique ownership ref in the same no-dereference ref transaction: definite failure never deletes a same-OID concurrent branch, while timeout/cancellation rollback requires the durable ownership ref. Source and target branch refs must be direct refs; symbolic refs are rejected. Failed/cancelled merge recovery only compare-and-sets the exact transaction-owned target ref; it never runs an unqualified hard reset after a separately checked symbolic HEAD, and retains index/worktree state whenever atomic recovery is impossible. Hook-driven branch switches therefore cannot reset an unrelated branch. Hook-capable checkout, pull, squash, and commit steps reverify symbolic HEAD, the direct target ref, and their exact OID binding. Stash creation first creates an unpublished object and then publishes that exact OID with a no-dereference compare-and-set against the captured `refs/stash`; a concurrent external push wins safely and is never captured as transaction-owned. Recovery applies the exact owned stash commit, then compare-and-sets or deletes `refs/stash` only if the stack stayed unchanged; a concurrent push after apply retains both stashes for recovery. A successful squash is accepted only when its index/final tree equals the merge tree of the captured target/source and its new commit has exactly the captured target parent; an already-integrated no-op is identified from the same trees, never command prose. Auto-commit stages tracked and untracked files and status failures deny merge. Cleanup never force-removes a dirty source; the legacy `--wt-force` setting is deprecated and non-destructive. After removal, one no-dereference `update-ref --stdin` transaction verifies the exact target ref and compare-deletes the exact source ref atomically. Operations sharing one canonical Git common directory serialize, while independent repositories remain concurrent. Legacy session and `/undo` Git sites retain their separately audited lifecycle gaps. | Shell-free argv and non-UTF8 path arguments are portable where the platform filesystem supports them. Unix whole-tree cleanup uses process groups; Windows direct-child cleanup is bounded, but descendant-tree parity remains an explicit gap. |
| `TC-SUPPORT-UTILITY` (direct argv) — `show_get_started` (`less`), Unix/macOS branches of `open_url` (`xdg-open`/`open`), `copy_to_clipboard` (`wl-copy`/`xclip`/`pbcopy`/`clip.exe`), `App::run_lazygit`, and the fixed `/usr/bin/sw_vers -productVersion` macOS containment gate | mini-agent selects the executable and fixed options. Human-selected document/URL/action remains one direct argv value; clipboard content is written to stdin. The macOS version probe has no user-authored values. No shell interprets these arguments. | Current process workspace is captured and explicitly applied by the hardened lazygit runner; `less` receives the resolved global documentation path. The version probe does not consume cwd. | Desktop utilities inherit the ambient session environment and credentials they require. The version probe clears its environment and receives no credentials. | Selected document/workspace plus desktop IPC; a browser opener may cause network access in the launched application. The version probe needs only system-version metadata and no network. | No broker containment. Lazygit is explicitly classified as `user-trusted-bypass`; requested restriction never silently falls back. The version probe runs in the trusted parent before a worker launch decision and failure leaves JS unavailable. | The initiating UI action authorizes launch. Lazygit emits utility kind, captured cwd, bypass boundary, and every probe/launch outcome without output. The inherited-stdio worker owns its audit metadata and emits the terminal outcome only after cleanup, including caller-drop cancellation; the remaining direct utilities still lack shared audit. Never record clipboard contents. The version probe has fixed identity and returns only a validated numeric major. | Lazygit's captured version probe and the macOS version probe have 5 s / 64 KiB bounds. Its inherited-stdio launch has a finite 24 h bound, caller-drop cancellation, and process-group kill/reap; tests cover timeout, caller drop, TERM-ignoring descendants, terminal cancellation audit, and a subsequent launch. **Current gap:** the other direct utility launch sites still lack a common lifecycle policy. | Executable lists differ by platform and missing tools fall through where documented. `less`, `xdg-open`, `open`, and root-owned `/usr/bin/sw_vers` are Unix/macOS paths; direct `clip.exe` is the Windows clipboard path. Unix lazygit tree teardown is tested; Windows parity remains unsupported. Unknown or failed macOS version probes disable JS. |
| `TC-SUPPORT-UTILITY` (opaque editor shell) — `InputEditor::open_in_editor` and `edit_memory_file_with_shell` | Human/configuration authors opaque editor shell text. mini-agent invokes `sh -c '<editor> "$1"' sh <temp-path>`; the temp path remains positional parameter `$1`, but the editor string is intentionally shell grammar. | Current process workspace; the selected temporary file is passed as `$1`. | Ambient shell/editor environment and credentials. | The selected temporary or memory file plus whatever desktop/project access the configured editor needs; network is editor-defined. | No broker containment. Any requested restriction is a support-utility policy and fails closed. | The editor action authorizes the configured shell program. Audit records editor identity, cwd, temporary-target class, and outcome, never file contents. **Current gap:** no shared audit. | Human-controlled interactive lifetime; requires cancellation and direct-child/tree reaping. **Current gap:** raw `.status()` has no common lifecycle policy. | Currently hard-coded to `sh`; this grammar is supported only where that shell contract exists. Windows editor launch is unsupported rather than portable direct argv. |
| `TC-SUPPORT-UTILITY` (Windows command interpreter) — `open_url` fallback `cmd /c start <url>` | mini-agent supplies `/c`, `start`, and the validated URL as separate process argv, but `cmd.exe` subsequently interprets command metacharacters. This is interpreter grammar, not direct argv safety. | Current process workspace. | Ambient Windows desktop/session environment and credentials. | Desktop shell/browser authority; opening the URL may cause browser network access. | No broker containment. Requested restriction fails closed. | The initiating UI action authorizes one HTTP(S) URL. **Current gap:** `is_safe_url` rejects whitespace/control characters but not all `cmd.exe` metacharacters, so a URL can change the interpreted command. Fix is tracked by P1 bead `mini-agent-x9tw`; F01 does not implement it. | The opener is spawned and waited synchronously with no common deadline or tree cleanup. | Intended for Windows. Until `mini-agent-x9tw` replaces or safely encodes the interpreter boundary, this path has an explicit command-injection gap and must not be described as direct argv-safe. |
| `TC-INTERNAL-VERIFICATION` — `verify_workflow_only_headless_relevance`; hidden Windows AppContainer runtime probe | mini-agent authors fixed Bash/PowerShell/native probe behavior from checked-in policy; no user/model text enters argv. | Repository workspace for workflow verification; fresh private fixtures for the Windows probe. | Workflow verification inherits ambient credentials; the Windows probe uses the helper's narrow environment and private profile storage. | The Windows probe covers explicit-root reads/writes, path and executable stability, zero capabilities, absent loopback exemption, failed IPv4/IPv6 TCP connections, omitted handles, descendant Job retention, and crash-stale cleanup. Zero capabilities plus the exemption check are the protocol-independent network proof; connectionless UDP send completion is not evidence of delivery. | Internal verifiers, not model or broker authority. Windows traverses the production helper, AppContainer attributes, handle ACLs, private desktop, creation-time Job, and parent-death path. | A fixed hidden verification flag is the audit identity; record policy version and outcome. | The Windows probe is CI-bounded and cleanup-attested; the workflow verifier's raw `.status()` remains a gap. | Workflow verification is Unix-only; the AppContainer runtime probe is Windows-only. |
| `TC-LIFECYCLE-HELPER` — `Sandbox::run_built_output_command`, `Sandbox::run_built_status_command`, and `sandbox::kill_process_group` | The owning caller supplies an already-constructed command; the helper adds no argv grammar or authority. | The owning constructor sets cwd. | The owning constructor defines environment policy. | The owning constructor defines filesystem/network authority. | Host lifecycle supervision does not change caller trust. | Audit belongs to the owning caller and survives caller drop. | Finite deadlines and output caps; Unix process-group signalling and Windows helper Job termination both drain descendants before completion. | Unix uses process groups; Windows delegates exact-tree termination to the AppContainer helper. |

`TEST-ONLY` identifies a launch expression inside a `#[cfg(test)]` block in a production source file;
it is checked for inventory drift but is not a production trust class. `NON-PROCESS` identifies a
lexical match that is a thread/task spawn, HTTP/tool status accessor, type reference, comment, or
the no-effect skill verifier's in-memory fake `spawn`; it never authorizes an OS process.

### Selected-workspace rebinding

The process-global current directory is only an initialization fallback. Startup captures one
canonical, identity-checked workspace binding and shares that authority across agent rebuilds;
changing worktrees never mutates process-global CWD. A worktree replacement is captured and
validated before state is committed, so a failed rebind retains the previous authority. The
selected session workspace is propagated explicitly to context discovery, the
permission root, model preambles, relative filesystem tools (including child `TaskTool` agents),
the model-action sandbox, JavaScript broker configuration, `!` shell commands, lazygit, `/undo`,
hook child CWD plus `ZEROSTACK_PROJECT_DIR`, and command-backed MCP transports. A worktree switch
restarts command-backed MCP servers in that workspace before rebuilding the agent.

The internal-Git dirty-worktree transaction refuses automatic merge recovery when any untracked
or ignored path is present, before a hard reset can remove an obstruction. For tracked changes it
publishes only the exact object returned by `stash create`. Recovery applies that exact object and
retains `refs/stash` unless both the current index and worktree still exactly match the stash's
index and worktree trees immediately before the compare-and-set deletion. Every source-worktree
removal also runs the bounded `ls-files --others -z` scan without exclusion rules; ignored or
untracked data, or a failed scan, retains both the worktree and its source branch.

## Broker-only JS worker is a separate boundary

`TC-BROKER-JS-WORKER` is reserved for the Phase 6 brokered JavaScript evaluator. It applies only to
the exact reviewed platform launcher and its required runtime attestation. Linux uses the enforced
empty-root bubblewrap path. Validated macOS 26 hosts use the one-time-image Seatbelt boundary with
typed `DeprecatedBestEffort` assurance; other macOS majors remain unavailable.
Windows may report available only after its cached minimal production attestation succeeds; the
hosted full canary remains separate release evidence. A design intent, general shared sandbox call,
or uncontained test launcher is not a current-class implementation.

The broker profile is narrower than `TC-MODEL-ACTION`: it hosts untrusted evaluation and asks the
parent broker to perform separately authorized effects. It is categorically incompatible with
`TC-MCP-STDIO`, `TC-LSP-SERVICE`, `TC-EXPLICIT-USER-SHELL`, `TC-LOOP-VALIDATION`, and
`TC-SUPPORT-UTILITY`, all of which legitimately need workspace, desktop, toolchain, credential, or
long-lived service authority. Shared lifecycle utilities may be reused, but a trust profile may
not be selected merely to reuse implementation code.

## Requested-backend state machine

Every class that offers optional containment represents state explicitly:

| State | Required behavior |
|---|---|
| Not requested | Launch only if the class permits ambient execution; report/audit that no backend is active. |
| Requested and available | Validate the backend identity and policy, construct the class-specific boundary, then launch once. |
| Requested but unavailable/invalid | Deny before child creation. Do not retry ambiently, choose another profile, or report configured capabilities as active. |

An explicit opt-out may select "not requested" only before launch and only for a class whose
contract permits it. A failed requested launch can never be reinterpreted as an opt-out.

## Exact launch-site audit

The checked inventory in `src/tests/subprocess_inventory_tests.rs` records the multiset of every
production-source line found by the required lexical audit:

```text
Command::new | tokio::process | .spawn( | .output( | .status(
```

Guarded process terminals normalize to their original lexical fingerprint so moving a site behind
the crate-wide creation boundary does not change its trust class. A separate inventory assertion
requires every Windows-capable `TC-*` terminal to use the guarded standard-library, Tokio, or RMCP
helper. That assertion parses imports and local type/module provenance while tokenizing complete Rust
sources, so whitespace-separated methods, qualified-angle UFCS, and renamed standard-library/Tokio
`Command` calls cannot evade it. Type aliases and local-module re-exports are resolved recursively;
glob imports and out-of-line modules remain opaque, including after a named import, and ambiguous or
cyclic provenance fails closed rather than inheriting a local-type exemption. Associated terminal
function-item references and raw terminal identifiers are inventoried even when a later indirect
call has another name. Terminal method identifiers in macro inputs and locally defined
`macro_rules!` expansion bodies are treated as process terminals unless an exact inventory
identity classifies the site as non-process. That identity binds the source path, occurrence, and
SHA-256 of the unambiguously framed full macro-context chain. Each invocation structurally encodes
the exact path tokens (including root qualification and raw identifier spelling), punctuation
character and spacing, token-tree kind, nested delimiter, and literal spelling; it never relies on a
reconstructed path or stringified token stream. Matching only the terminal line, inner invocation,
or macro name cannot confer an exemption. Only
syntactically proven task/thread or local associated `spawn` calls are excluded; ambiguous and
unrecognized terminals fail closed. Spawn/status helpers hold the Windows creation mutex only through
synchronous spawn. The output helper delegates to `std::process::Command::output` under the mutex so
explicit stdio and reusable-builder semantics remain exact; that synchronous helper can therefore
hold the mutex through output completion. Raw terminals in async functions, after `.await`, or in
deferred async/closure bodies cannot claim lexical guard dominance. A raw terminal nested in macro
arguments or a local macro expansion body also cannot claim dominance because expansion may defer
execution beyond the guard scope.
Target-specific Linux/macOS worker terminals and explicit `TEST-ONLY` sites are outside this Windows
race boundary.
`src/process_creation.rs` cannot assign a principal because it preserves the caller's class, so it
has a dedicated exact multiset inventory. That audit enumerates every raw standard-library, Tokio,
and RMCP terminal and requires the first statement in its owning helper to retain the crate guard
without moving or dropping it. New, duplicate, removed, or unguarded raw terminals fail.

The inventory excludes dedicated test directories, retains inline test and false-positive matches
as `TEST-ONLY`/`NON-PROCESS`, and assigns every remaining match to one current class above. An
explicit current-class allow-list permits `TC-BROKER-JS-WORKER` only at the reviewed Linux worker
fingerprints. Every disposition and every site
in a file with multiple production classes has an exact fingerprint-and-occurrence ownership rule;
only files whose remaining launches share one production principal use a file-family rule. Thus a
launch cannot satisfy the check by borrowing another site or comment's class token. The inventory
counts identical fingerprints per file, so adding a second identical `Command::new("git")` still
fails. A new or changed match fails the default test until its owner audits the full launch contract,
updates this normative table when needed, and adds the required site or single-class-family ownership
reference. Removing a site also fails until the stale inventory and ownership rule are removed.

The audit currently resolves as follows:

| Classification | Exact source families covered |
|---|---|
| `TC-MODEL-ACTION` | `src/sandbox.rs` opaque-shell/zerobox/Seatbelt/bwrap constructors and terminal `cmd.spawn()`; Bash and JS callers are named in the normative table. Shared direct-exec wrapper constructors are occurrence-owned separately. |
| `TC-BROKER-JS-WORKER` | Production bubblewrap constructor and terminal launches in `src/sandbox/worker/linux.rs`; inline adversarial child launches remain exact `TEST-ONLY` fingerprints. |
| `TC-EXPLICIT-USER-SHELL` | Delegating calls in `src/startup.rs` and `src/ui/app.rs::run_bang_command`; shared construction, capture, audit, and rendering live in `src/sandbox.rs`. The explicit-workspace regression helper in `src/ui/mod.rs` is occurrence-owned separately. |
| `TC-LOOP-VALIDATION` | Shared bounded runner in `src/extras/loop/validation.rs`; headless and interactive call sites contain no raw process constructor. |
| `TC-PROJECT-AUTOMATION` | Direct program plus the project-automation occurrences of zerobox/Seatbelt/bwrap constructors in `src/sandbox.rs`; terminal spawn in `src/extras/hooks/subprocess.rs`. |
| `TC-MCP-STDIO` | Tokio process import/type and RMCP terminal spawn in `src/extras/mcp/client.rs`. |
| `TC-LSP-SERVICE` | Tokio child/stdin types, constructor, and terminal spawn in `src/extras/lsp/client.rs`. |
| `TC-INTERNAL-GIT` | The Tokio constructor/import in `src/extras/git_worktree/mod.rs`, plus constructors/output terminals in `src/session/mod.rs` and the explicit-workspace stash helper in `src/ui/mod.rs`. |
| `TC-SUPPORT-UTILITY` | Direct-argv sites: pager in `src/docs.rs`, lazygit in `src/ui/app.rs`, the explicit-workspace lazygit helper in `src/ui/mod.rs`, opener/clipboard constructors in `src/ui/renderer.rs`, and the fixed macOS version probe in `src/sandbox/worker/macos.rs`. Opaque editor-shell sites: `src/ui/input/mod.rs` and `src/ui/slash/memory.rs`. Windows command-interpreter site: the `cmd /c start` opener fallback in `src/ui/renderer.rs`. |
| `TC-INTERNAL-VERIFICATION` | Fixed embedded-policy Bash constructor/status in `src/extras/loop/mod.rs`; fixed Windows AppContainer runtime probe constructors/terminals in `src/sandbox/windows.rs`. |
| `TC-LIFECYCLE-HELPER` | Shared bounded captured-process terminal and in-process Unix process-group signalling in `src/sandbox.rs`. Caller trust remains the caller's class; this helper grants no authority itself. |
| `TEST-ONLY` | Inline Bash-tool and loop-validation process-existence helpers, plus the unconfined protocol-pipe worker fixture in `src/sandbox/worker.rs`. |
| `NON-PROCESS` | Lexical exclusions in ACP, export/HTTP, JS runtime/skill/supervisor stderr-drain, verification-dispatch, idle-retirement, and Windows containment-preflight helper thread tasks, fake verification, source comments, and assertions that legacy loop callers contain no raw Tokio process constructor. |

## Review and change rules

- A new launch site must update the checked inventory in the same change.
- Every current launch classification must be in the current-class allow-list. `TEST-ONLY`,
  `NON-PROCESS`, and sites in mixed-production-class files require exact fingerprint-and-occurrence
  ownership; file-family ownership is valid only when every remaining launch has one production
  principal. `TC-BROKER-JS-WORKER` is valid only for the exact reviewed Linux worker fingerprints;
  no file-family rule or cross-family relabel may borrow it.
- A new trust class or broader authority must update this file before implementation.
- Moving an existing launch to a shared runner must preserve its class; sharing code never merges
  principals.
- Logs and errors must report requested/active containment distinctly and must not expose inherited
  environment values, clipboard data, editor contents, hook stdin, or credentials.
- Platform support is a verified runtime property. Compilation alone cannot upgrade an
  "unsupported" or "unverified" contract field.

## Acceptance criteria

- [x] Every current production lexical launch match maps to a stable trust class or an explicit
      non-process/test-only exclusion.
- [x] Every class records principal, argv, cwd, environment/credentials, filesystem/network,
      containment, permission/audit, resource/cancellation/tree, and platform semantics.
- [x] Requested containment failure is fail-closed and cannot silently downgrade authority.
- [x] The broker-only JavaScript worker is distinct from workspace services, explicit user
      commands, and the current shared model-action sandbox; no current launch may claim it.
- [x] The default test suite contains a drift check for newly added, changed, duplicated, or
      removed launch expressions.
